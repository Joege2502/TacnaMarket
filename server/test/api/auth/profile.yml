info:
  name: Get Profile
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3001/auth/profile
  headers:
    - name: Authorization
      value: Bearer {{token}}
      type: text
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
