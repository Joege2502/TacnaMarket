info:
  name: List All Users
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3001/user
  headers:
    - name: Authorization
      value: Bearer {{token}}
      type: text
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
